# Lifecycle Hooks

A lifecycle hook system that isolates page-specific logic from global events.

setup:

- Include the javascript ['/lifecycle-hooks/lifecycle-hooks.js'](/lifecycle-hooks/lifecycle-hooks.js) to UI Builder -> Settings -> Theme (Your Theme) -> Advanced -> Sub Header.
- You can also copy the content javascript directly to Custom Javascript section.